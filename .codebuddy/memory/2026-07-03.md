# 2026-07-03

## 搜索 UX 优化 — SkillPanel 显隐修复
- **问题**：SkillPanel 在搜索有结果时仍然显示，与 spec 要求不符（spec: "Panel hidden during active search"）
- **修复**：`apps/web/app/page.tsx` 中 SkillPanel 加上 `{!query && (...)}` 条件，与 TrendingSection 保持一致
- **影响**：当用户输入搜索词（query 非空）时，技能面板自动隐藏，搜索框下方只显示搜索结果和重新搜索按钮
